spring:
  application:
    name: company
  cloud:
    config:
      uri: ${vcap.services.config-get.credentials.uri}

management:
  security:
    enabled: false

security:
  basic:
    enabled: false